
## 26.06.2026

**Микропроект #1: заглушка**

- ✅ Объявили структуру `ContentView: View`
- ✅ Реализовали `body`
- ✅ Вызвали `ContentUnavailableView` с тремя параметрами
- ✅ Запустили и увидели красивый пустой экран

🔥 ЗАПОМНИ
| ContentUnavailableView — это готовый экран
| от Apple для случаев, когда данных нет.
| Он принимает три параметра: заголовок, иконку, описание.

**Создала сниппет `ProjectListView`:**
- Лежит в `Frontend/Shared/`
- Принимает: title, projects, content
- Использую для всех списков

**ПЕРВЫЙ ДЖЕНЕРИК!**

- Написала `struct ProjectListView<Content: View>: View`
- Поняла, что `Content` — это параметр типа
- Ограничила, что `Content` должен быть `View`
- Мой ментор сказал, что плавает в дженериках. Я — нет.
